Supervise exactly one bounded long-running NeuroGolf controller tick from the
cloned `rajshah4/openhands-agent-research-lab` repository.

Use the cloned repository path supplied in the repository context. Do not edit
code, inspect or print secrets, create a pull request, or perform any additional
research.

From the repository root, first run:

```bash
git fetch origin main
git checkout -B main origin/main
./experiments/in-platform-controller/automation/supervise-endurance-tick.sh start
```

The start command is idempotent. Never run `preset_tick.py` directly and never
launch a second controller process.

While the returned status is `running`, wait 45 seconds and run exactly:

```bash
./experiments/in-platform-controller/automation/supervise-endurance-tick.sh status
```

Repeat only that status check until it reports `completed`. Then run:

```bash
./experiments/in-platform-controller/automation/supervise-endurance-tick.sh result
```

If the exit code is zero, report the final JSON controller status from the log
and finish. Otherwise report the exit code and failing stage without printing
environment variables or credential values, then finish with a failure.
