steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file
      - experiments/agent-canvas-kubernetes/controller/Dockerfile
      - --tag
      - ${_IMAGE}
      - .
  - name: gcr.io/cloud-builders/docker
    args:
      - push
      - ${_IMAGE}
images:
  - ${_IMAGE}
